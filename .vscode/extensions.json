{
  // ★ステップ実行に要るのは C/C++ 拡張 (cppdbg)。ボタンは VS Code 標準の
  //   「Run and Debug」パネル (緑の ▶ / F5 / ステップのツールバー) を使う。
  //   cortex-debug は要らないが、あると SVD でレジスタを覗ける。
  "recommendations": ["ms-vscode.cpptools", "marus25.cortex-debug"]
}
