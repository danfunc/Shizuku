// ★このファイルはビルドシステムが生成する。手で編集しない。
//   番号を振っているのは cmake/shizukuObjects.cmake で、各コンポーネントは
//   shizuku_declare_objects(<component> <name>...) で名前を宣言するだけ。
//   **番号をどこにも書かない**ので、番号の衝突は起こり得ない (D28)。
#ifndef SHIZUKU_OBJECT_IDS_HPP
#define SHIZUKU_OBJECT_IDS_HPP
#include <cstdint>

namespace shizuku {
namespace object_id {

// 0 はブートスレッドが名乗るオブジェクト。以降は宣言順。
constexpr uintptr_t root = 0;
@SHIZUKU_OBJECT_ID_BODY@
constexpr uintptr_t count = @SHIZUKU_OBJECT_ID_COUNT@;

} // namespace object_id
} // namespace shizuku
#endif // SHIZUKU_OBJECT_IDS_HPP
