/.DS_Store
/build*/
/bazel-*

# ユーザーのグローバル gitignore が `.*` (ドットファイル全部) を除外しているため、
# ビルドに必須のドットファイルはここで明示的に戻す。
!.bazelrc
!.bazelversion
!.gitignore
# ★ビルドと書き込みのタスクはここ (Cmd+Shift+B で焼ける)。CMake を捨てたので
#   Pico 拡張のボタンの代わりになっている = リポジトリに要る。
!.vscode/
!.vscode/*
